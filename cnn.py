import numpy as np
from process import FEATURES_TYPE


class ChessValueDataset():

    def __init__(self):
        # Load data from files

        def __len__(self):
            return self.features.shape[0]

        def get(self, idx):
            return (self.features[idx], self.labels[idx])




